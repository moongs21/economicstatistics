# Git 명령어 실행 가이드

## ✅ 1단계: Git 설치 확인

PowerShell에서 다음 명령어를 실행하세요:

```powershell
git --version
```

버전 번호가 나오면 설치 완료입니다.  
오류가 나오면 아래 설치 방법을 따라하세요.

## 📥 2단계: Git 설치 (아직 안 했다면)

### 방법 A: 공식 웹사이트 (권장)
1. 브라우저에서 https://git-scm.com/download/win 접속
2. 자동으로 다운로드 시작됨
3. 다운로드한 `.exe` 파일 실행
4. 기본 설정으로 "Next" 클릭하여 설치
5. **PowerShell을 완전히 종료하고 다시 열기** (중요!)

### 방법 B: winget 사용 (Windows 10/11)
PowerShell을 **관리자 권한**으로 열고:

```powershell
winget install --id Git.Git -e --source winget
```

## 🔧 3단계: Git 초기 설정 (처음 한 번만)

Git 설치 후 처음 사용할 때:

```powershell
git config --global user.name "당신의 이름"
git config --global user.email "your-email@example.com"
```

예시:
```powershell
git config --global user.name "Moon Kee"
git config --global user.email "moonkee@example.com"
```

## 📂 4단계: 프로젝트 폴더로 이동

```powershell
cd C:\Users\cheong.moon.kee\.cursor\Study\251226_IMF
```

## 🚀 5단계: Git 명령어 실행

### 5-1. Git 저장소 초기화 (처음 한 번만)
```powershell
git init
```

### 5-2. 파일 추가
```powershell
git add .
```

### 5-3. 커밋 (변경사항 저장)
```powershell
git commit -m "Add Netlify Functions to fix CORS issue"
```

### 5-4. GitHub 저장소 연결 (처음 한 번만)
먼저 GitHub에서 저장소를 만들어야 합니다:
1. https://github.com 접속
2. "+" 버튼 → "New repository"
3. 저장소 이름 입력 (예: `imf-economic-dashboard`)
4. "Create repository" 클릭
5. 생성된 페이지에서 URL 복사

그 다음 PowerShell에서:
```powershell
git remote add origin https://github.com/당신의사용자명/저장소이름.git
```

예시:
```powershell
git remote add origin https://github.com/johndoe/imf-economic-dashboard.git
```

### 5-5. 브랜치 설정 및 푸시
```powershell
git branch -M main
git push -u origin main
```

## ⚠️ 주의사항

### 첫 번째 푸시 시 인증 필요
- 사용자 이름: GitHub 사용자명 입력
- 비밀번호: **Personal Access Token** 사용 (일반 비밀번호 아님)
  - GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
  - "Generate new token" → `repo` 권한 체크 → 생성
  - 생성된 토큰을 복사하여 비밀번호 대신 입력

### "remote origin already exists" 오류
이미 연결되어 있다는 의미입니다. `git push`만 실행하세요.

### "nothing to commit" 메시지
변경사항이 없다는 의미입니다. 파일을 수정한 후 다시 시도하세요.

## 📋 전체 명령어 순서 (한 번에 복사)

```powershell
# 프로젝트 폴더로 이동
cd C:\Users\cheong.moon.kee\.cursor\Study\251226_IMF

# Git 초기화 (처음만)
git init

# 파일 추가
git add .

# 커밋
git commit -m "Add Netlify Functions to fix CORS issue"

# GitHub 연결 (처음만, URL은 실제 값으로 변경)
# git remote add origin https://github.com/사용자명/저장소명.git

# 푸시
git branch -M main
git push -u origin main
```

## 🎯 다음 단계

GitHub에 업로드가 완료되면:
1. Netlify 대시보드로 이동
2. 사이트 설정 → Functions 탭 확인
3. `imf-api` 함수가 배포되었는지 확인
4. 사이트에서 데이터 불러오기 테스트




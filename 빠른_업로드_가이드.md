# 빠른 GitHub 업로드 가이드

## 🚀 빠른 시작 (Git이 설치되어 있다면)

PowerShell에서 프로젝트 폴더로 이동한 후:

```powershell
cd C:\Users\cheong.moon.kee\.cursor\Study\251226_IMF

# Git 저장소 초기화 (처음만)
git init

# 모든 파일 추가
git add .

# 커밋 (변경사항 저장)
git commit -m "Fix Netlify Functions endpoint and improve data parsing"

# GitHub 저장소 연결 (처음만, URL은 실제 값으로 변경)
# git remote add origin https://github.com/당신의사용자명/저장소이름.git

# 푸시 (GitHub에 업로드)
git branch -M main
git push -u origin main
```

## ⚠️ Git이 설치되어 있지 않다면

1. **Git 설치**: https://git-scm.com/download/win
2. 설치 후 PowerShell 재시작
3. 위 명령어 실행

## 🔍 문제 해결

### "fatal: not a git repository"
→ `git init` 먼저 실행

### "remote origin already exists"
→ 이미 연결됨. `git push`만 실행

### "Permission denied"
→ GitHub Personal Access Token 필요
- GitHub → Settings → Developer settings → Personal access tokens
- "Generate new token" → `repo` 권한 체크 → 생성
- 비밀번호 대신 토큰 사용

## ✅ 업로드 후 확인

1. GitHub에서 파일이 업로드되었는지 확인
2. Netlify 대시보드에서 자동 배포 시작 확인
3. 배포 완료 후 사이트에서 테스트



